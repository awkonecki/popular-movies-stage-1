package com.example.nebo.popular_movies.data;

import org.junit.Test;

import java.net.URL;

public class MovieDataModelTest {

    // Ignoring since having to do full mock of URL / URI classes.
    @Test
    public void test() {
        // URL url = MovieDataModel.buildPopularURL();

        assert (true);
    }
}
